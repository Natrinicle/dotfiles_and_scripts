# Writing Style Auto-Update

When the user corrects how you draft messages, commits, or PR text on their behalf, update `user_writing_style.md` under `{MEMORY_ROOT}` immediately.

Capture durable preferences (capitalization, punctuation, tone, list style). Skip one-off wording that is not a general rule. Re-draft with the new preference applied.
